<view class="waiting">
  <image class="qrcode" src="{{qrcode}}" mode="aspectFill" />
  <view class="users">
    <image wx:for="{{users}}" wx:key="index" class="avatar {{item === '' ? 'loading' : ''}}" mode="aspectFill" src="{{item}}" />
  </view>
</view>